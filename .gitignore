#ドット・ギットイグノア
# Terraform
# Terraform 作業ディレクトリ
.terraform/
# Terraform 状態ファイル
.terraform.tfstate
terraform.tfstate
terraform.tfstate.*
terraform.tfstate.backup
*.tfstate
*.tfstate.backup
# プランファイル
*.tfplan
*.plan
# 秘密情報を含む可能性のある変数ファイル
terraform.tfvars
*.tfvars
*.auto.tfvars
*.tfvars.json
# CLI 設定（トークン等を含む可能性あり）
.terraformrc
terraform.rc
# Terraform のクラッシュログ
crash.log

# VSCode
# 個人環境依存の設定は Git に含めない
.vscode/

# 共有したい場合は settings.json だけ除外しないなど
# .vscode/settings.json を残すなら ↑ をコメントアウトして ↓ に変更
# .vscode/*.*
# !.vscode/settings.json

# Java / Maven (app-backend)

target/
*.log

# Maven の内部ファイル
**/mvnw
**/mvnw.cmd
**/.mvn/

# Docker
# Docker の実行時生成物
**/docker-compose.override.yml
**/docker-compose.local.yml

# DB/ミドルウェアの永続化ディレクトリ（bind mount してる場合）
dbdata/
mysql-data/
postgres-data/
redis-data/

# Python（future-python-worker ができたとき用）
__pycache__/
*.pyc
.venv/
.env

# Ansible（特に秘密鍵やキャッシュ）

# Ansible の一時キャッシュ（存在する場合）
.ansible/
# SSH鍵ファイルをうっかり含めない
*.pem
*.key
# Ansible 実行時に生成されるファイル
*.retry
# Vault ファイル（使う場合）
*.vault
vault.yml

#############################################
# OS ごみ
#############################################
.DS_Store
Thumbs.db


*.txt

# db
init.sql
**/init.sql
